/* My comments */

#import "world.map"

	bool b1;
	bool b2;
	bool b3;
	char f1;
	int i1;
	bool v2[5, 7];
	int v3[42, 58];
	int v4[34];
	bool b4;
	bool b5;
	char s1;
	int i2;
	bool b6;
	int i3;
	char s2;
	char f4;
	int d;

    char fct1(int x)
    {

        x = 5;
    	x = x - i1;
        f1 = s2;
    	return f1;
    }

    int fct2(int x, bool  y)
    {
    	if( x >= 5) {

    	x = x - 1;
    	}
    	else
    	  {
    	   x = x + 1;
    	  }
     return x;
    }

    bool f3;

    bool fct3()
    {
        f3 = (true && false) || ((!true || (b1 || b2)) || v2[0, (i3+1-2*8)*0]);
      return f3;
    }

    char fct4()
    {
       f4 = 'a';
     return f4;
    }

    int fct5(int x, bool y, char z)
    {
    		if (y &&  x < 2) {
    			while (x < i3) {
    				x = x + 1;
    				}
    		}
    		else {
    			if (z == 'a') {
    				x = 2;
    			}
    		}
    	return x;
    }
void main()
{
int f;
char c;

 dig();
 dig();
 f = fct5(5, true, 'a');
 d = 10;
 c = fct1(d);
 dig();
 return void;
}
